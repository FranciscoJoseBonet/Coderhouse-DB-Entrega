<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>{{title}}</title>
		<style>
			body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background:
			#f0f2f5; color: #333; margin: 0; padding: 20px; } h1, h2 { text-align: center;
			color: #2c3e50; margin-bottom: 20px; } section { background: #ffffff; padding: 25px;
			margin-bottom: 25px; border-radius: 12px; box-shadow: 0 6px 15px rgba(0, 0, 0,
			0.08); transition: transform 0.2s, box-shadow 0.2s; } section:hover { box-shadow: 0
			10px 20px rgba(0, 0, 0, 0.12); } /* Product List */ ul#prodList { list-style: none;
			padding: 0; margin: 0; display: grid; grid-template-columns: repeat(auto-fill,
			minmax(280px, 1fr)); gap: 15px; } ul#prodList li { padding: 18px 15px; display:
			flex; flex-direction: column; gap: 6px; background: #fafafa; border-radius: 8px;
			box-shadow: 0 3px 10px rgba(0,0,0,0.05); transition: transform 0.2s, box-shadow
			0.2s, background 0.2s; } ul#prodList li:hover { background: #f1f6fc; transform:
			translateY(-3px); box-shadow: 0 10px 20px rgba(0,0,0,0.12); } li .title {
			font-weight: 600; color: #34495e; font-size: 1.05rem; } li .description { color:
			#555; font-size: 0.95rem; } li .meta { font-size: 0.85rem; color: #7f8c8d; display:
			flex; flex-wrap: wrap; gap: 8px; } li .price { color: #27ae60; font-weight: 700;
			font-size: 1rem; } .pagination { text-align: center !important; display: flex
			!important; justify-content: space-around !important; align-items: center;
			margin-top: 15px; } .pagination a, .pagination span { padding: 8px 14px;
			border-radius: 6px; border: 1px solid #3498db; text-decoration: none; font-weight:
			600; transition: all 0.2s; display: inline-block; min-width: 45px; text-align:
			center; } .pagination a { color: #3498db; background: white; } .pagination a:hover {
			background: #3498db; color: white; transform: translateY(-2px); } .pagination span {
			color: #2c3e50; background: #ecf0f1; } /* Active page number */ .pagination .active
			{ background: #3498db; color: white; border-color: #2980b9; } /* Form styles */ form
			{ display: flex; flex-direction: column; gap: 15px; } .fila { display: flex; gap:
			15px; flex-wrap: wrap; } .fila > div { flex: 1 1 200px; display: flex;
			flex-direction: column; } input, select { padding: 10px 12px; border: 1px solid
			#ccc; border-radius: 8px; font-size: 0.95rem; transition: border 0.2s, box-shadow
			0.2s; width: 100%; box-sizing: border-box; } input:focus, select:focus {
			border-color: #3498db; box-shadow: 0 0 5px rgba(52, 152, 219, 0.3); outline: none; }
			button { background: #3498db; color: #fff; border: none; padding: 12px 0;
			border-radius: 8px; cursor: pointer; font-weight: 600; transition: background 0.2s,
			transform 0.1s; width: 150px; align-self: flex-end; } button:hover { background:
			#2980b9; transform: translateY(-1px); } a { display: block; text-align: center;
			margin-top: 25px; color: #3498db; text-decoration: none; font-weight: 600;
			transition: color 0.2s; } a:hover { color: #2980b9; text-decoration: underline; }
		</style>

	</head>
	<body>

		<h1>Real-Time Products</h1>

		<section>
			<h2>Products List</h2>
			<ul id="prodList"></ul>
			<div id="pagination" class="pagination"></div>
		</section>

		<section>
			<h2>Add Product</h2>
			<form id="createForm">
				<div class="fila">
					<div>
						<label for="title">Title</label>
						<input id="title" name="title" type="text" placeholder="Title" required />
					</div>
					<div>
						<label for="description">Description</label>
						<input
							id="description"
							name="description"
							type="text"
							placeholder="Description"
						/>
					</div>
				</div>

				<div class="fila">
					<div>
						<label for="code">Code</label>
						<input id="code" name="code" type="text" placeholder="Code" required />
					</div>
					<div>
						<label for="price">Price</label>
						<input
							id="price"
							name="price"
							type="number"
							step="0.01"
							placeholder="Price"
							required
						/>
					</div>
				</div>

				<div class="fila">
					<div>
						<label for="stock">Stock</label>
						<input id="stock" name="stock" type="number" placeholder="Stock" required />
					</div>
					<div>
						<label for="category">Category</label>
						<input
							id="category"
							name="category"
							type="text"
							placeholder="Category"
							required
						/>
					</div>
				</div>

				<div class="fila">
					<div>
						<label for="status">Status</label>
						<select id="status" name="status">
							<option value="true" selected>Active</option>
							<option value="false">Inactive</option>
						</select>
					</div>
				</div>

				<button type="submit">Add</button>
			</form>
		</section>

		<section>
			<h2>Delete Product</h2>
			<form id="deleteForm">
				<label for="prodId">Product ID</label>
				<input id="prodId" name="prodId" type="text" placeholder="Product ID" required />
				<button type="submit">Delete</button>
			</form>
		</section>

		<p><a href="/">Go Home</a></p>

		<script src="/socket.io/socket.io.js"></script>
		<script src="../js/realTimeScript.js"></script>

	</body>
</html>